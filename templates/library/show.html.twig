{% extends 'base.html.twig' %}

{% block title %}Library{% endblock %}

{% block body %}
    <h1>Library</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ library.id }}</td>
            </tr>
            <tr>
                <th>Tarjeta_id</th>
                <td>{{ library.tarjetaId }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ library.nombre }}</td>
            </tr>
            <tr>
                <th>Seudonimo</th>
                <td>{{ library.seudonimo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_library_index') }}">back to list</a>

    <a href="{{ path('app_library_edit', {'id': library.id}) }}">edit</a>

    {{ include('library/_delete_form.html.twig') }}
{% endblock %}
